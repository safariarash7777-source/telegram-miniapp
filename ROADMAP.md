# نقشه راه توسعه «سوپر مینی‌اپ» آرش صفری

سند نیازسنجی و نقشه راه فنی. هدف: تبدیل مینی‌اپ فعلی به سوپر اپ مالی شخصی (Personal Finance) برای مخاطبان تلگرام.

> **به‌روزرسانی (تیر ۱۴۰۵ / Jul 2026):**
> - **فاز ۰ (امنیت و پی‌ریزی) انجام شد** — نشست سروری مبتنی بر initData، حذف کامل `telegramId` از ورودی‌ها، نقش ادمین، secret وب‌هوک، rate-limit/helmet، ایندکس‌ها. جزئیات در بخش فاز ۰ پایین.
> - **استراتژی هاستینگ عوض شد:** دیپلوی روی VPS شخصی با Coolify (`Dockerfile` + `DEPLOYMENT.md`). فونت/لوگو داخل ریپو vendor می‌شوند (`scripts/fetch-brand-assets.sh`) و وابستگی runtime به Manus حذف شد.
> - **پیامد زیرساختی:** به‌جای cron مانوس (`heartbeat.ts`) از node-cron داخل پروسه استفاده شود؛ به‌جای استوریج Forge برای فیش‌های پرداخت، والیوم لوکال Docker (`/data/uploads`)؛ نوتیف ادمین از طریق خود بات تلگرام.
> - **قانون سخت برای همه فیچرهای جدید:** هیچ پروسیجری هویت (`telegramId`) را از ورودی کلاینت نگیرد — همیشه از `ctx.telegramSession` (پروسیجرهای `telegramProcedure`/`telegramAdminProcedure` در `server/_core/trpc.ts`).

## Context — چرا این توسعه؟

اپ فعلی یک MVP ساخته‌شده با قالب Manus است (React 19 + tRPC 11 + Drizzle/MySQL + Express): قیمت لحظه‌ای طلا/سکه/ارز، ماشین‌حساب رشد سرمایه، پرتفوی ساده، فرم مشاوره، تحلیل‌ها و صفحه درباره من. تصمیمات محصولی نیازسنجی‌شده با مالک:

- **مدل درآمد:** فعلاً رایگان؛ بعداً اتصال به سایت. اما زیرساخت فروش وبینار و رزرو وقت مشاوره باید کامل آماده باشد.
- **پرداخت:** کارت به کارت + آپلود فیش + تایید ادمین (بدون درگاه).
- **اولویت قابلیت‌ها:** پرتفوی و ردیابی دارایی، هشدار قیمت، آکادمی/آموزش، ابزارهای مالی شخصی (حباب دارایی‌ها، صندوق‌ها و...).
- **پرتفوی:** طلا/سکه/ارز + بورس و صندوق‌ها + مسکن و خودرو (بدون کریپتو فعلاً).
- **وبینار:** لایو با لینک خارجی؛ فروش اصلی در سایت ساخته می‌شود، مینی‌اپ باید فضای ثبت‌نام/لینک داشته باشد.
- **رزرو مشاوره:** تقویم اسلاتی متصل به اکانت ادمین.
- **پنل ادمین:** داخل خود مینی‌اپ (نقش admin).

## وضعیت فعلی — یافته‌های کلیدی بررسی فنی

### نقاط قوت (قابل اتکا برای توسعه)
- اسکلت `server/_core` قوی است: tRPC typed + superjson، `protectedProcedure`/`adminProcedure` آماده اما استفاده‌نشده، JWT session با jose، هِلپرهای آماده LLM/Storage(S3)/Notification/Cron (`server/_core/heartbeat.ts`) — همه Forge-backed و بلااستفاده.
- `server/telegram-verify.ts` الگوریتم HMAC تایید initData را **درست** پیاده کرده (فقط استفاده نمی‌شود!).
- `price-service.ts` معماری چندمنبعه خوبی دارد (کانال تلگرام ← TGJU ← fallback) با کش ۵ دقیقه‌ای.
- دیزاین سیستم RTL/دارک navy-gold با فونت Pelak در `client/src/index.css` برند-محور و آماده گسترش است.

### مشکلات بحرانی (بلاکر توسعه — باید اول حل شوند)
1. **هویت جعل‌پذیر (IDOR):** همه ۱۵+ پروسیجر tRPC از نوع `publicProcedure` هستند و به `telegramId` خام ارسالی از کلاینت اعتماد می‌کنند. هر کسی می‌تواند پرتفوی/شماره تلفن/مشاوره‌های هر کاربر دیگر را بخواند یا حذف کند (`server/routers.ts`).
2. **`verifyInitData` هرگز صدا زده نمی‌شود** و نتیجه‌اش به session وصل نیست.
3. **Webhook تلگرام بدون احراز:** header مخفی `X-Telegram-Bot-Api-Secret-Token` چک نمی‌شود؛ `POST /api/telegram/set-webhook` هم بدون احراز است (`server/telegram-webhook.ts`).
4. **Admin secret هاردکد** با پیش‌فرض `"arash-safari-admin"` در `routers.ts:182`.
5. **هیچ rate-limit / helmet / پنل ادمین / زیرساخت پرداختی وجود ندارد.** `consultation.submit` بدون محدودیت → امکان flood کردن پیام به ادمین.
6. فرانت: بدون code-splitting (باندل تک‌تکه شامل ۱۴۰۰ خط ComponentShowcase مرده)، فونت گوگل runtime، کلاس‌های CSS تعریف‌نشده (`.card-elevated` و...)، ErrorBoundary انگلیسی با stack خام.

---

## نقشه راه — ۶ فاز

### فاز ۰: امنیت و پی‌ریزی — ✅ بخش سروری انجام شد

وضعیت: احراز هویت نشستی (`telegramAuth.login` + کوکی `tg_session`)، پروسیجرهای محافظت‌شده، نقش ادمین (`telegram_users.role` + auto-promote از `TELEGRAM_ADMIN_CHAT_ID`)، secret وب‌هوک، محافظت `set-webhook` با `ADMIN_SECRET`، حذف `adminSecret` هاردکد، helmet + rate-limit + کاهش body limit، ایندکس‌های `telegramId` و تست‌های authorization — همگی پیاده و تست شده‌اند. **باقی‌مانده از فاز ۰ (فرانت):** حذف کد مرده قالب (ComponentShowcase و...) و code-splitting per-route.

**احراز هویت واقعی تلگرام:**
- کلاینت در شروع، `initData` را به `telegramAuth.verifyInitData` بفرستد؛ سرور بعد از تایید HMAC (با `crypto.timingSafeEqual` و کاهش عمر به ~۱ ساعت)، یک **session JWT** با هِلپر موجود `sdk.ts` بسازد و در کوکی `app_session_id` بگذارد (زیرساخت کوکی/JWT در `server/_core/sdk.ts` و `cookies.ts` آماده است).
- `telegramProtectedProcedure` جدید در `server/_core/trpc.ts` که `telegramId` را از session بخواند نه از input. **همه** پروسیجرهای portfolio/consultation/profile به آن مهاجرت کنند و input های `telegramId` حذف شوند.
- جدول `telegram_users` ستون `role enum('user','admin')` بگیرد؛ `adminProcedure` تلگرامی بر اساس آن. تلگرام‌آیدی ادمین از `TELEGRAM_ADMIN_CHAT_ID` تشخیص و auto-promote شود. حذف `adminSecret`.

**سخت‌سازی:**
- Webhook: تنظیم `secret_token` در `setWebhook` و اعتبارسنجی header در هر update؛ حذف یا محافظت `POST /set-webhook`.
- `express-rate-limit` (سراسری + سخت‌گیرانه روی submit ها)، `helmet`، کاهش body limit از 50mb.
- ایندکس‌های DB روی `telegramId` جداول.

**پاکسازی فرانت:**
- حذف کد مرده: `ComponentShowcase`, `Home`, `AIChatBox`, `Map`, `DashboardLayout`, `ManusDialog`, `_core/useAuth` مسیر OAuth.
- `React.lazy` per-route + Suspense؛ self-host کردن فونت Vazirmatn؛ تعریف کلاس‌های CSS گمشده؛ فارسی‌سازی ErrorBoundary و NotFound.
- ساختار feature-based برای صفحات (`client/src/features/*`) تا افزودن ماژول‌های سوپر اپ `App.tsx` را منفجر نکند.

### فاز ۱: پنل ادمین داخل مینی‌اپ

- روت `/admin` با گارد role (فقط برای ادمین در BottomNav/منو ظاهر شود).
- بخش‌ها: **مشاوره‌ها** (لیست با `getAllConsultations` موجود در `db.ts` که الان بلااستفاده است + تغییر status که enum اش در schema هست ولی endpoint ندارد)، **تحلیل‌ها** (CRUD با ادیتور Markdown — رندر با `streamdown` که dependency موجود است)، **کاربران** (لیست/جستجو)، **پرداخت‌ها** (فاز ۳ به بعد)، **اسلات‌های مشاوره** (فاز ۵).
- نوتیف تلگرامی ادمین برای هر رویداد مهم (زیرساخت `notifyAdminNewConsultation` موجود در `server/telegram.ts` تعمیم یابد).

### فاز ۲: پرتفوی حرفه‌ای و ابزارهای مالی شخصی (قلب سوپر اپ)

**گسترش منابع قیمت (`price-service.ts`):**
- **بورس/صندوق‌ها:** اتصال به TSETMC (یا BrsApi.ir به‌عنوان منبع پایدارتر) برای قیمت سهام و NAV صندوق‌های طلا/درآمد ثابت. لیست صندوق‌های منتخب توسط ادمین قابل مدیریت باشد.
- **مسکن/خودرو:** بدون قیمت لحظه‌ای — ثبت دستی ارزش با یادآوری دوره‌ای به‌روزرسانی.
- انتقال محاسبه **حباب سکه** از کلاینت (ضرایب هاردکد در `Dashboard.tsx`) به سرور + تاریخچه قیمت در جدول `price_history` (اسنپ‌شات دوره‌ای با cron موجود `heartbeat.ts`) برای نمودارهای روند.

**پرتفوی جدید:**
- جدول `portfolio_assets` گسترش یابد: نوع دارایی (gold/coin/currency/stock/fund/realestate/vehicle/cash)، تاریخ خرید، قیمت خرید، **تراکنش‌محور** (جدول `asset_transactions` برای خرید/فروش چندباره یک دارایی) به‌جای رکورد تکی.
- داشبورد پرتفوی: ارزش خالص دارایی (Net Worth)، سود/زیان لحظه‌ای و تاریخی، نمودار تخصیص دارایی، مقایسه بازدهی با تورم/دلار/طلا (نقطه تمایز محصول).
- ابزارهای مالی شخصی: ماشین‌حساب فعلی + **بودجه‌بندی ساده ماهانه**، محاسبه‌گر خمس/مالیات طلا (اختیاری)، هدف‌گذاری پس‌انداز (Goal) با پیشرفت.

### فاز ۳: هشدار قیمت و موتور نوتیفیکیشن

- جدول `price_alerts` (کاربر، دارایی، شرط بالاتر/پایین‌تر از X، وضعیت).
- Cron (هِلپر آماده `server/_core/heartbeat.ts` — روت‌های `/api/scheduled/*`) هر ~۵ دقیقه قیمت‌ها را چک و از طریق **بات تلگرام** (`sendMessage` موجود) هشدار بفرستد. Dedupe تا هشدار تکراری نرود.
- خلاصه بازار روزانه اختیاری (opt-in) از طریق بات.
- گسترش بات: منوی دستورات `/price`, `/portfolio`, `/alert` تا بات هم به بخشی از محصول تبدیل شود.

### فاز ۴: زیرساخت پرداخت کارت به کارت + آکادمی/وبینار

**پرداخت (ماژول مشترک برای وبینار و مشاوره):**
- جداول `products` (نوع: webinar/consultation/course)، `orders`، `payments` (مبلغ، شماره کارت مقصد، تصویر فیش، وضعیت pending/approved/rejected، یادداشت ادمین).
- فلوی خرید: انتخاب محصول ← نمایش شماره کارت ← آپلود فیش (زیرساخت آماده `server/storage.ts` — S3 via Forge — که الان بلااستفاده است) ← نوتیف تلگرامی به ادمین ← تایید/رد در پنل ادمین ← نوتیف نتیجه به کاربر از طریق بات.

**آکادمی/وبینار:**
- صفحه آکادمی: لیست وبینارها (عنوان، توضیح، زمان، ظرفیت، قیمت/رایگان)، ثبت‌نام + پرداخت، و پس از تایید: نمایش لینک شرکت + ارسال لینک و یادآوری خودکار با بات (cron) قبل از شروع.
- ساختار طوری باشد که بعداً به API سایت اصلی (فروش دوره) وصل شود — لایه `products` جدا از منبع محتوا طراحی شود.
- محتوای آموزشی رایگان: بخش مقالات/ویدیو (لینک یوتیوب/آپارات) با دسته‌بندی و مسیر یادگیری ساده.

### فاز ۵: رزرو وقت مشاوره تقویمی

- جدول `consultation_slots` (تاریخ/ساعت/مدت/نوع/قیمت/وضعیت) که ادمین در پنل تعریف می‌کند.
- کاربر: انتخاب اسلات آزاد ← پرداخت (ماژول فاز ۴) ← رزرو قطعی بعد از تایید ادمین ← یادآوری خودکار بات برای طرفین (۲۴ ساعت و ۱ ساعت قبل).
- سازگاری با فرم فعلی: فرم درخواست آزاد (بدون اسلات) به‌عنوان مسیر جایگزین بماند و به workflow وضعیت‌دار (pending→confirmed→completed) وصل شود.

### فاز ۶: تجربه کاربری سوپر اپ و اتصال به سایت

- استفاده از قابلیت‌های بلااستفاده Telegram SDK: `CloudStorage` (ترجیحات کاربر)، `requestContact` (گرفتن شماره واقعی به‌جای تایپ دستی در ProfileSetup)، `shareMessage`/`shareToStory` (اشتراک تحلیل‌ها و کارت پرتفوی — موتور رشد ویروسی)، `showConfirm` برای حذف‌ها، `enableClosingConfirmation` در فرم‌ها.
- سیستم دعوت/رفرال با `start_param` بات + جدول `referrals`.
- SSO با سایت آینده: توکن امضاشده از مینی‌اپ به سایت (deep link) تا کاربر تلگرامی در سایت هم شناخته شود.
- یکپارچه‌سازی دیزاین: مهاجرت تدریجی صفحات از inline-style به کامپوننت‌های shadcn تم‌شده با توکن‌های navy/gold تا دو سیستم توکن موازی حذف شود.

## ترتیب اجرا و وابستگی‌ها

```
فاز ۰ (امنیت) ── پیش‌نیاز همه
  ├─ فاز ۱ (پنل ادمین) ── پیش‌نیاز فازهای ۴ و ۵
  ├─ فاز ۲ (پرتفوی/قیمت‌ها) ── مستقل، بیشترین ارزش کاربری فوری
  └─ فاز ۳ (هشدارها) ── وابسته به price_history/cron فاز ۲
فاز ۴ (پرداخت+آکادمی) ── وابسته به ۰ و ۱
فاز ۵ (رزرو مشاوره) ── وابسته به ۴
فاز ۶ (پالیش/اتصال سایت) ── تدریجی، موازی با بقیه
```

## فایل‌های کلیدی برای هر تغییر

- احراز هویت: `server/telegram-verify.ts`, `server/_core/trpc.ts`, `server/_core/sdk.ts`, `client/src/contexts/TelegramContext.tsx`
- API: `server/routers.ts` (باید به روترهای ماژولار شکسته شود), `server/db.ts`
- اسکیما: `drizzle/schema.ts` + migration های جدید (روابط FK هم تعریف شوند — `relations.ts` الان خالی است)
- قیمت‌ها: `server/price-service.ts`, `server/channel-scraper.ts`
- بات: `server/telegram.ts`, `server/telegram-webhook.ts`
- کرون: `server/_core/heartbeat.ts` (آماده، فقط باید روت‌های `/api/scheduled/*` ثبت شوند)
- آپلود فیش: `server/storage.ts` (آماده)
- فرانت: `client/src/App.tsx` (routing + lazy), `client/src/pages/*`, `client/src/index.css`

## ریسک‌ها و نکات برای Manus

1. **وابستگی به Manus Forge عمیق است** (storage/LLM/cron/notification) — چون توسعه با Manus ادامه می‌یابد، این وابستگی تعمداً حفظ شود، اما پرداخت و احراز تلگرامی نباید به Forge گره بخورد.
2. **اسکرپر کانال طلا شکننده است** (regex روی HTML صفحه t.me) — برای «سوپر اپ» یک منبع قیمت پشتیبان دوم (BrsApi یا مشابه) اضافه شود و cron قیمت را در پس‌زمینه گرم نگه دارد تا کاربر هیچ‌وقت روی request path منتظر اسکرپ نماند.
3. کش‌ها همه in-memory هستند — تا وقتی single-instance است مشکلی نیست؛ اگر مقیاس افقی شد Redis لازم می‌شود.
4. تست‌ها فقط ۲ فایل هستند؛ برای هر فاز، تست پروسیجرهای جدید (خصوصاً authorization و فلوی پرداخت) الزامی باشد.

## معیار تایید (Verification)

- هیچ پروسیجری `telegramId` را از input نگیرد؛ تست: فراخوانی با session کاربر A برای داده کاربر B باید Forbidden بدهد.
- Webhook بدون secret header باید 403 بدهد.
- فلوی کامل: ثبت‌نام وبینار ← آپلود فیش ← تایید در پنل ادمین ← دریافت لینک از بات، end-to-end تست شود.
- هشدار قیمت: ثبت هشدار، جهش قیمت شبیه‌سازی‌شده، دریافت پیام بات.
- Lighthouse/باندل: حذف کد مرده و lazy-loading باندل اولیه را باید محسوس کوچک کند.
